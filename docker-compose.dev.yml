services:
  app-dev:
    image: node:20-alpine
    working_dir: /app
    command: sh -c "npm ci && npm run start -- --host 0.0.0.0 --port 4200"
    ports:
      - "4200:4200"
    volumes:
      - .:/app
      - /app/node_modules
